// The Policies dashboard now renders via PolicyLibrary (the redesigned card
// grid). This module is kept only as the stable import path for the shared
// PolicyCard type used by the setup wizard and policy picker.
export type { PolicyCard } from "./policy-card";
