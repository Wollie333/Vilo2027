import { Placeholder } from "@/components/Placeholder";
import { t } from "@/i18n";

export default function GuestHome() {
  return (
    <Placeholder
      title={t("guest.findYourNextStay")}
      note="Browse, trending destinations and featured stays land in Phase 2 — wired to live directory data."
    />
  );
}
