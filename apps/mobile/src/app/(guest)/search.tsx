import { Placeholder } from "@/components/Placeholder";
import { t } from "@/i18n";

export default function GuestSearch() {
  return (
    <Placeholder
      title={t("guest.tabs.search")}
      note="Search, filters and results land in Phase 2 — wired to live directory data."
    />
  );
}
